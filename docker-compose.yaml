version: "3.9"

services:
  backend:
    build: ./server
    ports:
      - "5239:5000"
    container_name: backend

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    container_name: frontend
    depends_on:
      - backend